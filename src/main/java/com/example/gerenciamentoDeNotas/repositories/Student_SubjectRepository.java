package com.example.gerenciamentoDeNotas.repositories;

import com.example.gerenciamentoDeNotas.entities.Student_Subject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Student_SubjectRepository extends JpaRepository<Student_Subject, Long> {
}
